#!/bin/bash -e

# Make jemalloc the allocator for a smaller memory footprint on tiny machines.
if [ -z "${RUBY_YJIT_ENABLE}" ]; then
  export RUBY_YJIT_ENABLE="1"
fi
if [ -f /usr/lib/*/libjemalloc.so.2 ]; then
  export LD_PRELOAD="$(echo /usr/lib/*/libjemalloc.so.2)"
fi

exec "${@}"
