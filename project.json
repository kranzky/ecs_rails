{
  "id": "025",
  "name": "ECS Rails",
  "slug": "ecs-rails",
  "tagline": "An Entity-Component-System reimagining of ActiveRecord that stays idiomatic to Ruby on Rails",
  "status": "launched",
  "category": "labs",
  "target": "Rails developers who want composable, component-based domain models without inheritance or polymorphic-association sprawl",
  "stack": [
    "Ruby",
    "Ruby on Rails",
    "PostgreSQL"
  ],
  "links": {
    "github": "https://github.com/kranzky/ecs_rails",
    "website": "https://ecs-rails.kranzky.com",
    "linear": "https://linear.app/future-factory/team/ECS"
  },
  "mrrHistory": [],
  "log": [
    {
      "date": "2026-07-17",
      "summary": "Added ECS Rails to the portfolio as a placeholder under Developer Tools. An open-source Ruby gem that reimagines ActiveRecord around an Entity-Component-System architecture while remaining idiomatic to Rails. Each entity is a lightweight identity record composed from reusable, lazily persisted components that encapsulate both data and behaviour; components are queried and processed independently through Systems, enabling composable domain models, eliminating much of the need for inheritance and polymorphic associations, and letting background processing operate directly on relevant data. Ships as a production-ready gem plus a companion bulletin-board application demonstrating how complex Rails apps can be built from reusable components while retaining Rails conventions, DX, and ecosystem.",
      "humanMinutes": 0,
      "aiMinutes": 0,
      "costs": []
    },
    {
      "date": "2026-07-17",
      "summary": "Turned proposal.html into a specification and shipped the first two RFCs. Interviewed to settle the design, then wrote docs/architecture.md (the binding invariants), seven ADRs, eight RFCs sizing the v0.1 build, and a design backlog. Scaffolded the ecs_rails gem (MIT, Ruby 3.4.5 via rbenv, RSpec against real PostgreSQL) and a lean Rails 8.1 bulletin-board demo consuming it via a path: dependency, so the PROCESS.md gem-to-demo feedback loop has no bundle step in it. Delegated RFC-0001 (ApplicationEntity, entity identity, model discriminator) and RFC-0002 (reload-safe component registry) to parallel sub-agents; 55 examples green. The agents found four defects in the spec itself, all now corrected: attr_readonly does not raise in Rails 8 without a hand-rolled guard; a per-subclass default_scope would accumulate and silently break sub-subclasses; RFC-0002 said registration was both idempotent and raising; and the registry's name-keying makes anonymous entity classes unregisterable, which invalidated RFC-0002's and RFC-0004's own example tests. Two new open questions logged, one of which blocks RFC-0003: entities.model does not resolve back to a subclass on read, so ApplicationEntity.find returns an ApplicationEntity rather than a User \u2014 undercutting ADR-0002's core justification.",
      "humanMinutes": 20,
      "aiMinutes": 95,
      "costs": []
    },
    {
      "date": "2026-07-17",
      "summary": "Phase 2: RFC-0003 (ApplicationComponent, entity subclass resolution) and RFC-0008 (install and component generators) built by parallel sub-agents. 157 examples green. Half of v0.1 is now landed (RFCs 0001, 0002, 0003, 0008). The headline finding is that ADR-0008 was wrong on both counts and had to be amended: overriding discriminate_class_for_record is dead code because ActiveRecord gates the hook on the result set containing inheritance_column, which the entities table has no reason to have \u2014 so Option A required the very gate that only the rejected Option B opens; fixed via the private instantiate_instance_of seam, now logged as an open question about depending on private Rails API. Separately, the ADR worried about the wrong failure mode: every irregular inflection round-trips fine (people, data, equipment, series, analyses), but namespacing is total and unfixable by inflection, because model_name.plural underscores the whole constant path before the inflector is consulted and is not injective (Blog::Post and BlogPost both give 'blog_posts'). Switched the discriminator to model_name.collection, which inverts cleanly and is byte-identical for every non-namespaced class, so no data migration was needed. The generator agent also found two require bugs inherited from Rails' own generator files, masked by an unrelated require in registry.rb. Both ADR errors were internally coherent, would have survived review, and were only caught by execution.",
      "humanMinutes": 15,
      "aiMinutes": 120,
      "costs": []
    },
    {
      "date": "2026-07-18",
      "summary": "Phase 4: RFC-0006 (lazy components) and RFC-0005 (method delegation) landed, sequentially \u2014 I'd twice mislabelled these as independent siblings, but delegation writers (user.address = 'x') need user.email to be an instance, and 0004's reader returns nil, so 0005 genuinely depends on 0006. Corrected the dependency graph. 339 examples green. The whole proposal now works end to end: user.email is never nil, reading it costs one SELECT and inserts nothing, user.email.address = 'x'; user.save! writes exactly one row, user.send_welcome_email delegates with self bound to the component, and name clashes between components raise at load time with the fix quoted. Two more ADR-grade findings, both from running the code rather than reading it. First, ADR-0003's claim that dirty-tracking 'follows ActiveModel rather than inventing new semantics' is provably false: a virtual component has entity_id set, which ActiveModel counts as a change, so deferring to changed? would insert a row for every component ever read \u2014 the gem must define its own dirty rule that excludes identity columns. That is the second approved ADR (after 0008) falsified by its own dependency; both assumed Rails already did the work. Second, the test harness itself was hiding rollbacks: spec_helper wrapped each example in a joinable transaction, so inner transactions merged instead of taking savepoints and rollbacks were swallowed \u2014 every atomicity assertion in the suite had been passing whether the code rolled back or not. Fixed with joinable: false, confirmed load-bearing. Also: except:/only: had to cover the whole accessor pair (title implies title=) or models.rb's own declaration could not load, and the delegated-set formula the RFC suggested would have delegated ~140 AR-generated dirty-tracking methods. Only RFC-0007 (validation error merging) remains for v0.1; the save/save! contract is already correct and atomic, so 0007's job is narrowed to making valid? reflect component validity and merging errors for forms.",
      "humanMinutes": 20,
      "aiMinutes": 210,
      "costs": []
    },
    {
      "date": "2026-07-18",
      "summary": "Phase 5: RFC-0007 (validation error merging) landed \u2014 v0.1 is now API-complete, all eight RFCs implemented, 382 examples green. entity.valid? reflects its touched components' validity and errors merge so a form reads normally (user.errors[:'email.address'] => ['is invalid'], full_messages => ['Email address is invalid']). Scope was narrower than the RFC first stated, because RFC-0006 had already made save/save! correct and atomic; the job reduced to making valid? fail first rather than the cascade's component.save! raising, and decoupling the machine-readable error key from the human message (which ActiveModel couples). valid? validates only components already in the entity's memo, and provably has no side effects. The agent surfaced the registry-singleton landmine a third time \u2014 its first cut read the registry from human_attribute_name, a hot per-form-field method, which is fragile in production because the Railtie clears the registry on reload; fixed by keying on the durable generated reader method instead. I then sealed the root cause centrally: added Registry#snapshot/#restore and a global spec_helper hook that restores the models.rb baseline after every example, so no spec can leak a cleared registry regardless of order. Also untracked .claude/settings.local.json, which the harness kept rewriting. Next: build the bulletin-board demo for real against the finished gem \u2014 the actual v0.1 kill/keep question of whether modelling with components is pleasant.",
      "humanMinutes": 15,
      "aiMinutes": 130,
      "costs": []
    },
    {
      "date": "2026-07-18",
      "summary": "Demo phase (models + sharp cases), driven iteratively with a friction log as the deliverable \u2014 the v0.1 kill/keep question of whether modelling with components is pleasant. Modelled the whole bulletin board (Users, Posts, Comments, Groups, Memberships) against the finished gem. The verdict: the core claims hold cleanly \u2014 shared component types with shared behaviour (Likes on Post+Comment, Name on User+Group), relationship components, and the join-entity pattern for many-to-many all just work. The demo drove two real gem improvements. First, a \ud83d\udd34 infinite-recursion bug: the gem's OWN ADR-0006 example (class Author; belongs_to :author) stack-overflowed, because the component reader `author` and the delegated belongs_to `author` collided and the delegation silently overwrote the reader with a method that called itself; fixed to raise a reader collision at declaration (ADR-0004 stance), and the fix pointed at a nicer model (name the component Authorship, the association author, so post.author returns the User). Second, a \ud83d\udd34 blocker for the proposal's 'No STI' headline: marker components (Moderator/Administrator) have no state, so the lazy cascade never persists them and user.moderator; user.save! silently did nothing. Added a first-class presence API (ADR-0009/RFC-0009): entity.add/has?/remove plus a generated entity.<component>? predicate. Markers now work \u2014 a user is a moderator exactly when the row exists. Also decided to keep attribute auto-delegation automatic (collisions raise loudly, so except:-on-both is the documented pattern) rather than make it opt-in. Gem at 424 examples green. Two ADRs amended by their own demo: ADR-0006 (relationship naming) and ADR-0009 (predicate collisions defer, not raise). Next: optionally build controllers/views, or consolidate.",
      "humanMinutes": 30,
      "aiMinutes": 240,
      "costs": []
    },
    {
      "date": "2026-07-19",
      "summary": "Built the component query DSL (with_component / without_component) \u2014 the proposal's hardest feature and the demo's #1 confirmed need. RFC-0010 + ADR-0011, implemented via a sub-agent, 457 gem examples green. Two demo-surfaced constraints drove the design and both are handled. First, verb naming: the proposal's .with and .without are BOTH already ActiveRecord methods (CTEs and the excluding alias), and even .composed_of is taken (aggregations) \u2014 so the whole proposed query syntax collides with Rails; the free, unambiguous with_component/without_component verbs are used instead. Second, cross-entity leakage: component tables are shared across entity types (PublishState on Post and Group), so a naive component query is blind to entity type; the DSL builds from the entity's own default-scoped relation and adds only a correlated EXISTS/NOT EXISTS, so the entity-model scope (model = 'posts') applies automatically and a shared component cannot leak. Proven with Name (shared by User and Post in the gem fixtures) and in the demo with PublishState shared into Group. EXISTS chosen over JOIN (no duplicate rows, clean AND composition, null-safe NOT EXISTS). Conditions are sanitized through AR's where (verified a quote-bearing payload is escaped, not executed). The demo's Post.published was rewritten from the hand-rolled subquery (whose correctness silently rode the default_scope, a trap caught in review) to with_component(PublishState, state: 'published'). This closes the top backlog item; preloading (N+1) remains the next confirmed need.",
      "humanMinutes": 15,
      "aiMinutes": 110,
      "costs": []
    },
    {
      "date": "2026-07-20",
      "summary": "Built component preloading (includes_components) \u2014 the last confirmed demo need, closing architecture.md open question 1. RFC-0011 + ADR-0012, 468 gem examples green. A spike up front reframed the whole feature: because components are has_one associations and the lazy reader (RFC-0006) calls super, ActiveRecord's NATIVE preload already works \u2014 User.all.preload(:name) drops 5 queries to 2 and a preloaded no-row component still reads as a virtual. So the feature is not machinery but a thin, discoverable, component-named wrapper: includes_components takes component classes (no args = all declared, walking inheritance), preloads via AR, validates each is declared on the entity with a component-shaped error rather than AR's AssociationNotFoundError, and chains. Half its value is regression tests pinning the native-preload/lazy-reader interaction so it can't silently break. The demo index now preloads its own components plus the nested author-name hop (preload(authorship: { author: :name }), standard AR nesting for a component reached through a relationship). The payoff is that it is bounded, not merely smaller: 2 posts 13->6 queries, 22 posts 93->6. The real HTTP index request went 14->8, constant in post count. The agent also caught two bugs in my RFC's own tests (an all-components example reading u.avatar.url on User, which doesn't declare Avatar; and an error-shape test stubbed with 'if false' that asserted nothing) \u2014 both fixed. v0.1 plus its two demo-driven enhancements (query DSL, preloading) are done; remaining backlog items (relationship DSL, required components, non-equality query conditions, Systems) are enhancements, not blockers.",
      "humanMinutes": 10,
      "aiMinutes": 95,
      "costs": []
    },
    {
      "date": "2026-07-20",
      "summary": "Tagged the v0.1 milestone and wrote it up. ECS Rails v0.1 is feature-complete (11 RFCs, 12 ADRs, 468 examples against real PostgreSQL) and demo-validated by a working bulletin board built entirely on the gem; publishing to RubyGems remains a deliberate later step per ADR-0007, so the portfolio status stays 'building'. Deliverables: docs/retrospective-v0.1.md (the hypothesis 'is modelling a real Rails app out of components pleasant?', answered yes, with the process learnings and the roadmap), refreshed root and gem READMEs showing the real working API (lazy components, presence/markers, with_component/without_component, includes_components), and CHANGELOG 0.1.0. The retrospective's honest headline: across the build the design was wrong far more often than the code, and only execution caught it \u2014 two approved ADRs (0003, 0008) falsified by their own dependencies, an infinite recursion in the gem's own ADR-0006 example, and a test harness that silently swallowed rollbacks. The most compelling next item is the relationship DSL (the demo's Authorship/MemberUser/MemberGroup all reinvent belongs_to boilerplate, and it would enable a nested preloader for the last remaining N+1), deferred until the demo showed what it should look like \u2014 which it now has.",
      "humanMinutes": 10,
      "aiMinutes": 60,
      "costs": []
    },
    {
      "date": "2026-07-21",
      "summary": "Built the relationship DSL (relates_to) \u2014 the one backlog item the demo actively argued for \u2014 and used it to clean up the demo. RFC-0012 + ADR-0013, superseding ADR-0006's deferral, 512 gem examples green. relates_to :author, User on an entity declares a cross-entity link with NO relationship component file: the DSL dynamically defines a backing component (Post::AuthorRelationship, table post_authors) with the belongs_to and declares it, so the whole stack (registry, lazy reader, delegation, with_component, presence, includes_components) applies for free. post.author / post.author= reach the target via delegation; a generator emits only the migration. The two FKs are deliberately asymmetric: cascade on the owner side (entity_id), nullify on the target side (author_id) \u2014 deleting the User nullifies the link but the Post survives, proven against the real DB. A spike up front confirmed feasibility before design. The agent found a real ADR bug: the backing reader is author_relationship only because relates_to pins the backing class's model_name to the demodulized element; the naive derivation yields post_author_relationship (namespace leaks in, owner name doubles), which would have silently broken the preload key. It also added InvalidRelationship < InvalidComponent (relationship-shaped message, still caught by rescue InvalidComponent). Demo cleanup: deleted authorship.rb/member_user.rb/member_group.rb, rewrote Post/Comment/Membership with relates_to (Membership is now three lines), regenerated the backing migrations, and confirmed the index still serves both published posts with author names in a bounded 8 queries. Next per the user: polish the demo UI to look great, be idiomatic Rails, and expose all features.",
      "humanMinutes": 15,
      "aiMinutes": 130,
      "costs": []
    },
    {
      "date": "2026-07-21",
      "summary": "Polished the demo into a full, browsable bulletin board on a hand-written CSS design system (no framework), light/dark aware, exposing every gem feature. Built People (with moderator/admin badges = marker components, and promote/demote buttons wired to add/remove), Groups + Memberships (the join entity, add/remove members with a role), Comments and Likes on posts, and a 'how it works' explainer mapping each screen to the ECS model. Added Comment relates_to :post so comments belong to posts. Queries use the DSL throughout: a post's comments, a group's members, and a user's posts are all with_component on a relationship's backing column, and the index preloads to a bounded query count. Verified every flow end to end in the browser in both light and dark themes \u2014 post/comment/like creation, marker promote/demote (moderator? flips true/false), and an invalid email on the new-person form surfacing the merged component error (errors[:'email.address'] => 'is invalid'). One UI-phase finding logged: querying by a relationship leaks the backing class name (Comment::PostRelationship) and its raw column (post_id), which concretely argues for the relationship-name query sugar deferred from RFC-0012 \u2014 added to the backlog. Gem unchanged at 512 examples. This completes the user's two-part plan (relationship DSL, then UI polish); the demo now both models fully and looks the part.",
      "humanMinutes": 20,
      "aiMinutes": 210,
      "costs": []
    },
    {
      "date": "2026-07-21",
      "summary": "Added relationship-name query & preload sugar (with_related / without_related / includes_related) \u2014 the item the full UI surfaced. RFC-0013 + ADR-0014, 534 gem examples green. relationship queries previously had to name the backing component and its raw FK column (Comment.with_component(Comment::PostRelationship, post_id:)), re-exposing exactly what relates_to hides; now Comment.with_related(:post, post) reads by relationship name. It's thin sugar: relates_to records relationship metadata (name -> backing class name, FK, target name) stored by NAME and constantized on read (reload-safe like the registry, ancestry-walked), and with_related(:author, x).to_sql is byte-identical to the with_component form. The agent gave a sharp correction: my 'no cross-entity leak' claim was theatrical \u2014 relationship backing tables are owner-scoped by construction (post_authors vs comment_authors are disjoint), so relationship queries are leak-proof regardless of the entity-model scope, unlike a shared component table; corrected the ADR. Demo cleanup: rewrote the controllers to use with_related, removing every *Relationship backing-class name from app/controllers (two-hop author/member name preloads stay explicit per the RFC non-goal, using only the reader symbol). All pages verified rendering. This closes the last demo-surfaced item; remaining backlog is enhancement (non-equality query conditions, required components, Systems) with no demo yet arguing for it.",
      "humanMinutes": 10,
      "aiMinutes": 95,
      "costs": []
    },
    {
      "date": "2026-07-23",
      "summary": "Launched. The demo bulletin board is live at https://ecs-rails.kranzky.com (Fly.io, HTTPS, hourly self-reset with a live countdown), linked from every page to the GitHub repo and the gem's RubyGems page. Prepared the gem for its first RubyGems release: README doc links rewritten to absolute GitHub URLs (relative ../docs links break on rubygems.org), gemspec metadata polished (source_code_uri, changelog_uri, bug_tracker_uri, rubygems_mfa_required), and the status banner refreshed (534 examples, live demo link). A pre-flight name check caught that ecs-rails (hyphen) is already taken on RubyGems by an unrelated AWS-ECS gem, so renamed the gem to ecs_rails (underscore) \u2014 which is free and strictly better: it matches the EcsRails module and the ecs_rails require path exactly, so the lib/ecs-rails.rb require shim was deleted (no API change; module/require/classes all unchanged). Bumped to 0.2.1, amended ADR-0007's naming section. The gem builds cleanly as ecs_rails-0.2.1.gem; 534 examples green. Remaining manual step (needs the user's RubyGems credentials): gem push ecs_rails-0.2.1.gem after this is merged and tagged v0.2.1, then a small follow-up to repin the demo Gemfile from ecs-rails v0.2.0 to ecs_rails v0.2.1.",
      "humanMinutes": 10,
      "aiMinutes": 90,
      "costs": []
    },
    {
      "date": "2026-07-23",
      "summary": "Renamed the GitHub repo rails-ecs -> ecs_rails and retargeted every reference (17 URLs across 8 files: gemspec homepage/metadata, gem README doc links, demo Gemfile git source and lockfile, project.json, footer), plus the local git remote; rewrote the naming narrative in ADR-0007 and both READMEs from 'the repo deliberately differs' to 'one name everywhere'. Also fixed the demo footer and About links, which pointed at rubygems.org/gems/ecs-rails \u2014 the unrelated gem, not ours. Then gem push failed: RubyGems rejected ecs_rails as 'too similar to an existing gem named ecs-rails'. It compares names after collapsing -, _ and case, so ecs-rails, ecs_rails and ecsrails are one name, owned by a still-maintained AWS ECS gem (0.0.8, Oct 2025) \u2014 so the adoption path is closed and no spelling of 'ecs rails' is publishable. Checked eight alternatives for normalized availability and published as ecs_on_rails, a play on Ruby on Rails that keeps the rails keyword without the rails- prefix convention reserves for Rails Core. The rename stops at the gemspec by choice: module EcsRails, require path ecs_rails, generator namespaces and repo all unchanged, since propagating a third name through ~390 occurrences would churn the public API to fix a packaging accident. A one-line lib/ecs_on_rails.rb shim keeps Bundler.require working. Builds as ecs_on_rails-0.2.1.gem; 534 examples green.",
      "humanMinutes": 15,
      "aiMinutes": 75,
      "costs": []
    },
    {
      "date": "2026-07-24",
      "summary": "Published the gem and finished the launch. gem push of ecs_rails was rejected \u2014 RubyGems compares names after collapsing -, _ and case, so ecs-rails, ecs_rails and ecsrails are one name, owned by a still-maintained AWS ECS gem (0.0.8, Oct 2025); adoption is closed, so no spelling of 'ecs rails' is publishable. Published as ecs_on_rails instead, a play on Ruby on Rails keeping the rails keyword without the rails- prefix reserved for Rails Core. The rename stops at the gemspec by choice: module EcsRails, require path ecs_rails, generators and repo unchanged, with a one-line lib/ecs_on_rails.rb shim for Bundler.require \u2014 propagating a third name through ~390 occurrences would churn the public API to fix a packaging accident. Shipped 0.2.1, then found the published Changelog link 404d (the gemspec assumed a repo-root CHANGELOG.md, but the gem lives in gem/ of a monorepo) and gem metadata is immutable once published, so 0.2.2 corrects it, repoints source_code_uri at the gem/ subtree, and adds documentation_uri. Also added YARD API docs across the entire public API (100% documented, verified by yard stats): @param/@return/@raise/@example/@see on every public module, class, attribute and method, preserving the existing prose. Two encoding traps on the way: YARD reads .yardopts as BINARY so a non-ASCII --title crashes the layout template, and redcarpet returns ASCII-8BIT and fails the same way on em-dashes, so kramdown backs the markdown. --embed-mixins was load-bearing: Entity gains its whole DSL by extending DSL/Querying/Preloading/Relationships and none of it renders without that flag. Finally repinned the demo from the git source to the published ecs_on_rails ~> 0.2.2, dropping the git dependency from the Docker build. 534 gem examples green.",
      "humanMinutes": 20,
      "aiMinutes": 145,
      "costs": []
    },
    {
      "date": "2026-09-02",
      "summary": "Reframed the v2 roadmap around a sharper, falsifiable claim: after `rails g ecs_rails:install` and one `db:migrate`, building an application requires no further migrations. Reviewed all eleven open Linear issues against that goal and found the set coherent but short of it: three things still generated a migration after install (a table per component via the catalogue-as-generators plan in ECS-9, a table per `relates_to`, and a table per marker), and the video script in ECS-10 still ran `db:migrate` after composing entities. Argued the pros and cons of the fix the user then accepted: one shared `relationships` table keyed by (entity_id, slot) with `owner_model` and an `exclusive` flag under a partial unique index, so `has_one` stays DB-enforced with no per-relationship index (precedent: `active_storage_attachments`; equivalent to today in expressiveness, enforcement and query shape, differing only in costing zero migrations). Wrote ADR-0017 (shared relationships table, superseding ADR-0013's storage, amending ADR-0014's leak-proof argument) and ADR-0018 (the catalogue ships in the gem as concerns; install writes one-line app classes and one migration for every catalogue table; `ecs_rails:upgrade` is the only migration a user ever runs afterwards; markers become slots of one Marker component with a `marker` declaration, amending ADR-0009's rejection of that keyword; the component generator stays as the escape hatch). Rewrote RFC-0015 over the shared table and un-gated it \u2014 the marketplace design already lists the six manys and the orphan bug, so inverse relationships are built up front. Revised the marketplace design (build order, decisions, no demo-local components), the backlog and the docs index. In Linear: created ECS-14 (this design), ECS-15 (relationships on the shared table), ECS-16 (markers), ECS-17 (rebuild the forum on the catalogue only, so db/migrate holds one file), ECS-18 (demo back on a path dependency), ECS-19 (0.3.0 release), ECS-20 (blog post \"The Last Migration\"); reworked ECS-9 into the catalogue-in-the-gem, rebased ECS-6, widened ECS-7 to the whole first catalogue (~17 components), re-scripted ECS-10 so the only migrate is right after install, raised ECS-11 and shipped its slot syntax first, and moved ECS-2 out of the launch scope. Critical path: ECS-12 \u2192 ECS-4 \u2192 ECS-15/16 \u2192 ECS-9 \u2192 ECS-6 \u2192 ECS-5 \u2192 ECS-17 \u2192 marketplace \u2192 ECS-11 \u2192 ECS-19 \u2192 ECS-20/ECS-10.",
      "humanMinutes": 25,
      "aiMinutes": 75,
      "costs": []
    },
    {
      "date": "2026-09-04",
      "summary": "Implemented ADR-0016: delegation is component-prefixed by default (Linear ECS-12), with ECS-18 along for the ride. Gem: `component Foo` now delegates `#{reader}_#{method}` (`user.email_address`, `user.email_send_welcome_email`) through a single delegation map that conflict detection, reader-collision detection and method generation all read; `prefix: false` opts a declaration back to bare names, `prefix: true` is the explicit default, and a Symbol raises until RFC-0014's slots exist. The open question was settled by the user for a uniform prefix (verbs included; ugly verbs are reached via the reader or renamed). `only:`/`except:` keep naming the component's own methods. `relates_to` declares its backing component bare so `post.author` keeps its shape. Conflicts now need two bare declarations, so Name#title and Group#title coexist with no option. Flat mass assignment falls out for free and is pinned: `User.create!(name_first:, email_address:)` routes, dirties and persists through the cascade, `update!` too, unknown and stale bare keys still raise, and Rails multiparameter date_select fields route as well (the issue suspected they would not). delegation_spec rewritten; 564 examples green (was 534), YARD 100%. Demo: switched to `path: \"../gem\"` (ECS-18; Fly deploys pause until 0.3.0, documented in the Gemfile, Dockerfile and DEPLOY.md), dropped `except: [:text]` from Post and Comment, `PublishState` took `prefix: false` for `post.state`, every view/helper/controller moved to prefixed names and every create collapsed to flat mass assignment, including the seed. Verified by booting the server and exercising every page and write flow with curl; caught that a blank form field arrives as \"\" and dirties a lazy component, fixed with `.presence` and logged as a papercut. Docs: architecture.md \u00a74 rewritten, RFC-0005 amendment, ADR-0016 implementation notes, friction-log v2 section, CHANGELOG Unreleased, gem README, root README (corrected the stale 'not yet published' note), and a project CLAUDE.md.",
      "humanMinutes": 10,
      "aiMinutes": 70,
      "costs": []
    },
    {
      "date": "2026-09-04",
      "summary": "Implemented labelled (plural) components \u2014 slots \u2014 per RFC-0014 / ADR-0015 (Linear ECS-4). Gem: `component Address, prefix: :business` declares the component into slot \"business\" with reader `business_address` and delegation `business_address_line1`; every component table now carries `slot string NOT NULL DEFAULT ''` with the unique index moved to (entity_id, slot), and every has_one is slot-scoped (the default slot too, or a labelled row would answer for the singular reader). The lazy reader presets slot on a virtual, the dirty rule and delegation treat slot as identity, presence takes `prefix:` and is slot-scoped in SQL, queries take `prefix:` as sugar for `slot:`, preloading follows declarations so `includes_components(Address)` batches every slot, and validation keys are per reader. Registry declarations gain slot/reader_name/prefix/slot_options and key by (entity, component, slot); `Entity.declaration_for` added; `Entity.components` lists a type once. Added `delegate: false` (reader and predicate only) and a reverse reader-collision check. Settled ECS-4's slot-configuration question with `slot_option :name, default:` on the component plus extra keywords on `component`, resolved per instance through the owning entity's declaration (the same class is configured differently on different entities), with unknown options and shadowing names raising at declaration time. Corrected RFC-0014's examples: the reader rule gives `business_postal_address` for PostalAddress, so the fixture and examples are `Address`, the short name ADR-0016 freed. Generators: component and relationship migrations emit the slot column and composite index; new `rails g ecs_rails:upgrade` (ADR-0018's one post-install migration) inspects the database and writes one migration for every component table lacking slot, proven against a real 0.2.x-shaped table. 649 examples green (was 564), YARD 100%. Demo: `ecs_rails:upgrade` migrated all 17 tables; Group declares Description twice (description + :rules house rules) with form, view, seed and preload; verified by booting and posting through the form. The generator exposed three stale tables in the demo's schema.rb (authorships, member_users, member_groups) left from before relates_to \u2014 dropped, schema re-dumped, logged in the friction log. Docs: RFC-0014 amendment, ADR-0015 status, architecture \u00a71/\u00a72/\u00a74, docs index, backlog, CHANGELOG, gem README, CLAUDE.md.",
      "humanMinutes": 5,
      "aiMinutes": 80,
      "costs": []
    },
    {
      "date": "2026-09-04",
      "summary": "Implemented the shared relationships table per ADR-0017 (Linear ECS-15). Gem: every `relates_to` is now `component Relationship, prefix: name, delegate: false` \u2014 a row in one `relationships` table created at install, under a slot named for the relationship \u2014 with the four accessors (`author`, `author=`, `author_id`, `author_id=`) defined by the DSL and forwarding to the slot-scoped reader's `target`. `Relationship` is the app's one-line class including the new `EcsRails::Catalogue::Relationship` concern (ADR-0018's shape, the first catalogue entry), written by `ecs_rails:install` and found via the new `config.relationship_class_name`. Target type is checked in Ruby on assignment (`InvalidRelationship`); `owner_model` and `exclusive` are stamped before save; new `relates_to ..., unique: true` makes a link one-to-one via the install-time partial unique index. Relationship names reserve their accessors so later bare delegations conflict. `with_related`/`without_related`/`includes_related` compile to slot-scoped `with_component` on Relationship; nested preloads name `target`. Removed the dynamic backing classes, the per-relationship tables, and the `ecs_rails:relationship` generator. The install migration is now `ecs_rails_install` and creates both tables; `ecs_rails:upgrade` gained a data-moving job that creates `relationships` and copies every ADR-0013 backing table (recognised by shape and name) into it, proven against real 0.2.x-shaped tables. 672 examples green (was 649), YARD 100%. Demo: the upgrade generator moved all five backing tables in one migration with every link intact; only the two raw nested preloads changed. Docs: architecture \u00a72/\u00a75 rewritten, RFC-0012 and RFC-0013 amendments, ADR-0017 implementation notes, CHANGELOG, READMEs, backlog, CLAUDE.md, friction log.",
      "humanMinutes": 5,
      "aiMinutes": 75,
      "costs": []
    },
    {
      "date": "2026-09-04",
      "summary": "Implemented markers on one shared table per ADR-0018 \u00a74, written up as RFC-0016 (Linear ECS-16). Gem: `marker :moderator` is `component Marker, prefix: :moderator, delegate: false` plus a bare `moderator?` predicate and a Boolean `moderator=` writer (so a checkbox routes through flat mass assignment); `add`/`has?`/`remove` accept the marker name as a Symbol; `User.with_marker`/`without_marker` are slot-scoped sugar over the component verbs; marker names reserve their predicate and writer so components and relationships cannot take them, and `relates_to` now also checks the writer. `Marker` is the app's one-line class including the new `EcsRails::Catalogue::Marker` concern (the second catalogue entry after Relationship), written by install and named by `config.marker_class_name`; the install migration creates `markers`; `ecs_rails:upgrade` gains a third job that creates the table and moves every empty per-marker table into it. `has?` now answers from a loaded has_one, present or absent, so `includes_components(Marker)` makes a page of badges free. 708 examples green (was 672), YARD 100%. Demo: Moderator and Administrator component files, specs, tables and migrations replaced by two `marker` lines; one generated migration moved the rows; controllers use `add(:moderator)`; views unchanged; verified by booting and promoting/demoting through the UI. Also filed Linear ECS-21 (component-driven UI: catalogue components shipping display and form partials) at the user's request, with a backlog entry, and closed ECS-15 after its merge. Docs: RFC-0016, ADR-0018 \u00a74 and ADR-0009 notes, architecture \u00a72/\u00a74, docs index, backlog, CHANGELOG, READMEs, CLAUDE.md, friction log.",
      "humanMinutes": 5,
      "aiMinutes": 55,
      "costs": []
    }
  ]
}
